 <section class="container card-group">
  <% @purchases.each do |purchase| %>
    <div class="row">
      <div class="col-sm-4">
        <div class="card">
          <div class="card-block">
            <%= link_to artwork_path(purchase.artwork) do %>
              <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path purchase.artwork.photo_artwork, height: 300, width: 400,crop: :fill %>')">
              </div>
            <% end %>
          </div>
        </div>
      </div>
      <div class="col-sm-4">
        <div class="card">
          <div class="card-block">
            <div class="purchase-title">
              Title: <%= purchase.artwork.title %>
            </div>
            <div class="purchase-items">
              Description: <%= purchase.artwork.description %>
            </div>
            <div class="purchase-items">
              Price: € <%= purchase.artwork.price %>
            </div>
            <div class="purchase-items">
              Price: <%= purchase.artwork.price %>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</section>
